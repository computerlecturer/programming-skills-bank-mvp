# Python/Java/Linux 학습하기 메서드·명령어 보강 보고서

## 수정 기준
- 문제에 실제 등장하는 함수, 메서드, 키워드를 기준으로 학습하기를 보강했습니다.
- 정답을 그대로 알려주기보다 사용법, 반환 자료형, 원본 변경 여부, 풀이 접근 순서를 설명했습니다.
- 해설과 학습하기의 역할을 분리했습니다. 해설은 해당 문제의 풀이 근거, 학습하기는 문제 풀이 전 개념 정리 역할입니다.

## Python 보강
- 수정 문항 수: 66문항
- 추가 학습 요소: split(), join(), find(), replace(), lower(), upper(), strip(), len(), append(), insert(), pop(), sort(), sorted(), remove(), add(), keys(), values(), items(), type(), __name__, range(), reversed(), zip(), enumerate(), isinstance(), ord(), chr(), list(), set() 등
- Python 12번 해설에 type(a).__name__ 설명을 명확히 반영했습니다.

## Java 보강
- 수정 문항 수: 143문항
- 추가 학습 요소: print/println/printf, equals(), new String, length(), charAt(), substring(), toUpperCase(), toLowerCase(), split(), interface, implements, extends, super, this, ArrayList, add(), get(), size(), for/while/switch 흐름 등

## Linux 보강
- 수정 문항 수: 106문항
- 추가 학습 요소: pwd, ls, whoami, echo, clear, date, cd, mkdir, rm, cp, mv, cat, grep, chmod, man, sudo, touch, head, tail, df, du, ps, top, kill, export, 절대/상대 경로, ./, ../, 권한 숫자 계산 등

## 검토 결과
- study_guides_reviewed.json JSON 파싱 정상
- Python 문제 JSON 파싱 정상
- 기존 SQL 수정 내용과 Python 허용 정답 보강 내용은 유지했습니다.
